// TODO: Create admin repository
// - getDashboardStats(): COUNT queries for dashboard + recent users and events
// - getLogs({ page, limit }): paginated activity logs with user JOIN
// - findAllUsers({ page, limit, role, status }): paginated user list
// - findUserById(id): single user
// - updateUser(id, data): UPDATE user with dynamic fields
// - findAllEvents({ page, limit, status }): paginated events with organizer JOIN
