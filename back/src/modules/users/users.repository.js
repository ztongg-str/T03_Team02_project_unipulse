// TODO: Create users repository
// - findById(id): SELECT user by id (exclude password)
// - update(id, data): UPDATE user with dynamic fields
// - findAll({ page, limit, role, search }): paginated SELECT with optional filters
