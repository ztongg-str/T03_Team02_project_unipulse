// TODO: Define streaks routes
// GET / - authenticate
// GET /all - authenticate, authorize('admin', 'organizer')
// POST / - authenticate, authorize('admin', 'organizer')
// PUT /:id - authenticate, authorize('admin', 'organizer')
// DELETE /:id - authenticate, authorize('admin')
