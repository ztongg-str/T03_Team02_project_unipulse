// TODO: Create events repository
// - findAll({ page, limit, category, status, search }, user): paginated JOIN with filters
// - findUpcoming({ page, limit }): future approved events
// - findByOrganizer(organizerId, { page, limit }): events by organizer
// - findById(id): single event with organizer name JOIN
// - create(data): INSERT event
// - update(id, data): UPDATE with dynamic fields
// - remove(id): DELETE event
