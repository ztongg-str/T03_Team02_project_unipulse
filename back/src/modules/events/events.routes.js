// TODO: Define events routes
// GET / - authenticate
// GET /upcoming - authenticate
// GET /my - authenticate, authorize('organizer')
// GET /:id - authenticate
// POST / - authenticate, authorize('organizer', 'admin'), validate body
// PUT /:id - authenticate, authorize('organizer', 'admin')
// DELETE /:id - authenticate, authorize('organizer', 'admin')
// PATCH /:id/approve - authenticate, authorize('admin')
// PATCH /:id/reject - authenticate, authorize('admin')
// PATCH /:id/ban - authenticate, authorize('admin')
