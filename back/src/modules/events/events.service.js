// TODO: Create events service
// - getAllEvents(query, user): list with filters, students see only approved
// - getUpcomingEvents(query): future approved events sorted by date
// - getMyEvents(organizerId, query): events created by this organizer
// - getEventById(id): single event, 404 if not found
// - createEvent(data): insert new event with status 'pending'
// - updateEvent(id, data, user): owner or admin only, filter allowed fields
// - deleteEvent(id, user): owner or admin only
// - approveEvent(id): set status to 'approved' (admin)
// - rejectEvent(id): set status to 'rejected' (admin)
// - banEvent(id): set status to 'banned' (admin)
