// TODO: Create reports repository
// - findById(id): single report with reporter name JOIN
// - findAll({ status, page, limit }): paginated list with filters
// - create(data): INSERT report
// - update(id, data): UPDATE with dynamic fields
// - remove(id): DELETE report
