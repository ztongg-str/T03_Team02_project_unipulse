// TODO: Create registrations service
// - registerForEvent(userId, eventId): check event exists + approved + not full + not duplicate, create registration
// - cancelRegistration(id, user): check exists, owner or admin can cancel
// - getMyRegistrations(userId): list user's registrations with event details
// - getEventRegistrations(eventId, user): check event exists, organizer or admin can view
