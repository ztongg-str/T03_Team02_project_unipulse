// TODO: Create registrations repository
// - findByUserAndEvent(userId, eventId): find specific registration
// - countByEvent(eventId): COUNT registrations for an event
// - findById(id): single registration with user and event JOIN
// - create(userId, eventId): INSERT registration
// - remove(id): DELETE registration
// - findByUser(userId): all registrations for a user with event details
// - findByEvent(eventId): all registrations for an event with user details
