// TODO: Create auth service
// - register(data): check duplicate email/username, hash password, create user, return token + user
// - login(data): find user by email, compare password, return token + user
// - getMe(userId): find user by id, return user (without password)
