// TODO: Create input validation middleware
// - validate(schema) middleware that checks req.body, req.params, req.query
// - Export helper validators: required, isEmail, minLength, isIn
// - Return 400 with error messages if validation fails
