import { pool } from "../utils/database.js";

export async function getByUser(userId) {
  // TODO: return activity logs for user
  return [];
}

export async function log(userId, action) {
  // TODO: insert activity log entry
}
